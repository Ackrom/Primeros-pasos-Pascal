program sopa_de_letras;
uses crt;
type
matriz=array[1..25,1..20] of char;

program llenarMatriz(var a:matriz);
var x,y:integer;	
begin
reset(archivo);

for y:=1 to 20 do
	begin
		for x:=1 to 25 do
			begin
				
			end;
	end;
close(archivo);
end;

begin
end.
